import { describe, it, expect } from 'vitest'

describe('Penalties Store', () => {
    it('TODO: Write tests for penaltiesStore', () => {
        // Fail until tests are written
        expect(1).toBe(2);
    });
});
